{% extends "base.html.twig" %}
{% block title %}AppBundle:User:registration{% endblock %}
{% block body %}
{% include 'header.html.twig' %}
<div class="uk-container uk-width-xlarge uk-margin">
    <div class="uk-panel uk-panel-box uk-text-break">
    {% trans %}
        Вы попали на эту страницу потому, что забыли пароль!
        Для восстановления пароля необходимо ввести свой email.
    {% endtrans %}
    </div>
    <div class="uk-container uk-width-xlarge uk-text-center">
        <div class="uk-modal-header">
            <h2 class="uk-modal-title">{% trans %}Восстановление пароля{% endtrans %}</h2>
        </div>
        <div class="uk-modal-body">
            <form method="POST" action="{{path('login')}}">
                <div class="uk-margin">
                    <div class="uk-inline">
                        <span class="uk-form-icon" uk-icon="icon: mail"></span>
                        <input name="email" class="uk-input uk-width-large" type="text" placeholder="Введите email">
                    </div>
                </div>
                <div class="uk-margin">
                    <div class="uk-inline">
                        <button class="uk-button uk-button-primary" type="submit">Отправить</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
{% include 'footer.html.twig' %}
{% endblock %}
